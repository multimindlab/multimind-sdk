# MultiMind SDK Examples Documentation

This page is outdated (it references removed modules). The runnable, maintained examples live in the [examples directory](../../examples/README.md) at the repository root; task-oriented recipes are in the [cookbook](../cookbook.md).
