# Model Management Examples

This page is outdated (it references removed modules such as `multimind.models.advanced`). See the runnable examples in [examples/model_management](../../examples/model_management/) and the provider-switching recipes in the [cookbook](../cookbook.md).
