# Python Compatibility Fixes

This was a historical note about a 2024 CI issue with Python 3.13 and spaCy pins; it no longer reflects the current dependency setup. Supported Python versions and dependencies are defined in [pyproject.toml](../pyproject.toml); see [CHANGELOG.md](../CHANGELOG.md) for release history.
